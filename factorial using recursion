#include <stdio.h>
int fact(int n){
    int facto=0;
    if(n==0)
    {
       return 1;
    }
    else
    {
     return  n*fact(n-1);
    }
}

int main()
{
    int n;
    printf("enter a number:");
    scanf("%d",&n);
   int res=fact(n);
     printf("Factorial of a given number:%d",res);
}
